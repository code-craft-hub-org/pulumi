name: hello-app
runtime:
  name: nodejs
  options:
    typescript: true
description: >
  GCP Compute Engine infrastructure for hello-app.
  Stacks: staging (ephemeral, created/destroyed per deploy) | production (persistent).
